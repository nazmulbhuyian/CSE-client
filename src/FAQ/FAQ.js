import React from 'react';
import { useLoaderData } from 'react-router-dom';

const FAQ = () => {
    
    return (
        <div>
            <h1>This is FAQ</h1>
        </div>
    );
};

export default FAQ;